name: refinup_model_registry
description: Model registry and metadata management
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  refinup_ai_interface:
    path: ../refinup_ai_interface

dev_dependencies:
  flutter_test:
    sdk: flutter
