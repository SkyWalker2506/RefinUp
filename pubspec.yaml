name: refinup
description: RefinUp - Modular, BYOK-First Idea Refinement Platform

publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

# Dependency overrides (for monorepo consistency)
dependency_overrides:
